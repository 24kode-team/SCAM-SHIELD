import type { Metadata } from "next";
import { Geist_Mono } from "next/font/google";
import "./globals.css";

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Scam Shield Canada - Protect Yourself From Digital Scams",
  description:
    "Free tool to detect CRA scams, fake banking pages, phishing SMS, and other digital fraud targeting Canadians and newcomers.",
  keywords: "CRA scam, Canada scam, phishing, fraud detection, newcomers Canada",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistMono.variable} antialiased`}>{children}</body>
    </html>
  );
}
